/**
 * Horizontal breathing room before t=0 and after duration end in the main waveform.
 * Applied as margin on WaveSurfer's wrapper only — does not shift audio time or regions.
 */
export const WAVEFORM_HORIZONTAL_GUTTER_PX = 72;
